HLS 200-880 location explanations

Compute controller path
compute_controller.cpp:165 - This `read_i32()` call pulls activation data from the shared `in_buf` inside the pipelined MAC loop, so HLS sees a loop-carried memory dependence against other reads from the same buffer.
compute_controller.cpp:170 - This `read_i8()` path is another per-iteration access into the same `in_buf`, so mixing it with the other activation and weight reads prevents HLS from proving an II=1 schedule.
compute_controller.cpp:184 - This `read_i4()` weight fetch reads a nibble from the same backing buffer as the activation reads, creating a dependence between different byte-lane accesses in the same pipelined loop.
compute_controller.cpp:857 - The `MAC_OP_TO_BUF(...)` call keeps the whole buffer-reading MAC kernel inside the FSM execute state, so the scheduler has to carry the shared-buffer hazards through this state machine boundary.
top_params.hpp:524 - `read_i8()` is just a direct `buf[byte_addr]` load, so every call becomes a raw memory access that HLS must conservatively serialize with the other `in_buf` reads.
top_no_debug.cpp:343 - This top-level call passes the shared buffer pointers into `compute_controller`, so the read conflicts seen inside the controller are exposed on one common interface instead of separate independent memories.

MMU path
mmu_luka.cpp:563 - Reading `free_spans[bank_i][i]` while the same loop also compacts entries back into `free_spans` creates a read-after-write hazard on the same array.
mmu_luka.cpp:566 - Writing `cur` back into `free_spans[bank_i][write_idx++]` during the scan causes a loop-carried dependence because later iterations may still need data from that array.
mmu_luka.cpp:1129 - `uram_write_byte()` starts with a read of the current URAM word, so every byte write becomes a read-modify-write sequence on the same storage element.
mmu_luka.cpp:1131 - This masked update keeps the previous URAM word value live while building the new one, which is part of the same read-modify-write dependence chain.
mmu_luka.cpp:1132 - Writing the updated word back to `uram_banks` completes the read-modify-write cycle, so adjacent loop iterations cannot be freely overlapped on the same URAM word.
mmu_luka.cpp:1149 - `unpack_uram_word()` writes multiple bytes into `dst`, so HLS sees repeated stores into the same destination buffer across loop iterations.
mmu_luka.cpp:1150 - The extracted byte temporary on this line feeds the repeated `dst[...]` stores, so it is part of the same carried store dependence reported around the unpack path.
mmu_luka.cpp:1172 - `zero_region_chunks()` falls back to byte writes through `uram_write_byte()`, which means the loop performs repeated read-modify-write accesses to URAM and cannot sustain II=1.
mmu_luka.cpp:1212 - The byte-write path in `region_write_bytes()` updates URAM one byte at a time through `uram_write_byte()`, creating the same URAM read-modify-write hazard as above.
mmu_luka.cpp:1249 - The aligned read path expands one URAM word into many `dst` byte stores, so HLS detects destination-buffer store ordering dependencies in the pipelined loop.
mmu_luka.cpp:1257 - The fallback read path also stores into `dst[copied]`, so it conflicts with the aligned unpack path and contributes to the carried dependence on the output buffer.

Top-level path
top_no_debug.cpp:15 - `dma_buf_set_byte_u32()` begins by reading the current 32-bit staging word, so each byte update becomes a read-modify-write on `dma_rx_buf_local`.
top_no_debug.cpp:17 - This bit-mask merge updates one byte lane inside the already-read staging word, which keeps the read-modify-write dependence alive through the helper.
top_no_debug.cpp:18 - Writing the modified word back to the staging buffer means consecutive byte updates cannot be scheduled independently on the same local word.
top_no_debug.cpp:464 - Reading `kv_cache[idx]` into `beat` creates a bus read dependency that HLS later pairs with the writeback to the same AXI word.
top_no_debug.cpp:467 - Writing `beat` back to `kv_cache[idx]` after modifying one lane forms a bus-level read-modify-write sequence, which is why the warning mentions request/response dependence on `kv_gmem`.
top_no_debug.cpp:477 - Writing received bytes into `dma_rx_buf_local` calls the staging-buffer helper repeatedly, so the loop inherits the same local read-modify-write hazard from lines 15 to 18.
